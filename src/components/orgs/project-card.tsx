import * as React from "react";

type ProjectCardProps = {
  title: string;
  description: string;
  link: string;
}

export const ProjectCard: React.FC<ProjectCardProps> = () => {
  return null;
};